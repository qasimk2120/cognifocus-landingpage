{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/copy-static.cjs && node scripts/generate-sitemap.cjs && node scripts/generate-rss.cjs",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.18.1"
  }
}
