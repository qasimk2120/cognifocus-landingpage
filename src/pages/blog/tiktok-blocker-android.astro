---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("tiktok-blocker-android");

if (!post) {
  throw new Error("Missing blog post content for tiktok-blocker-android");
}
---

<BlogPostLayout post={post} />
