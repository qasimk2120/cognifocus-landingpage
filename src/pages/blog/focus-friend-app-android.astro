---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("focus-friend-app-android");

if (!post) {
  throw new Error("Missing blog post content for focus-friend-app-android");
}
---

<BlogPostLayout post={post} />
