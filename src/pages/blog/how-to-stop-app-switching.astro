---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("how-to-stop-app-switching");

if (!post) {
  throw new Error("Missing blog post content for how-to-stop-app-switching");
}
---

<BlogPostLayout post={post} />
