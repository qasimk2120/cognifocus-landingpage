---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("best-app-blocker-android");

if (!post) {
  throw new Error("Missing blog post content for best-app-blocker-android");
}
---

<BlogPostLayout post={post} />
