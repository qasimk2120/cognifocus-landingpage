---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import posts from "../../data/blog/posts.json";

const post = posts.find((entry) => entry.slug === "best-app-blocker-android");

if (!post) {
  throw new Error("Missing blog post data for best-app-blocker-android");
}
---

<BlogPostLayout post={post} />
