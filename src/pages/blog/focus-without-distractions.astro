---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("focus-without-distractions");

if (!post) {
  throw new Error("Missing blog post content for focus-without-distractions");
}
---

<BlogPostLayout post={post} />
