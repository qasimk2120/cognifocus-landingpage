---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import { getPostBySlug } from "../../utils/blog.js";

const post = await getPostBySlug("cognifocus-vs-stay-focused");

if (!post) {
  throw new Error("Missing blog post content for cognifocus-vs-stay-focused");
}
---

<BlogPostLayout post={post} />
