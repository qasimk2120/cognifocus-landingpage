---
import BlogPostLayout from "../../components/blog/BlogPostLayout.astro";
import posts from "../../data/blog/posts.json";

const post = posts.find((entry) => entry.slug === "focus-companion-app-android");

if (!post) {
  throw new Error("Missing blog post data for focus-companion-app-android");
}
---

<BlogPostLayout post={post} />
